# ansible-starter

## ansible cli
1. `. venv/bin/activate`
1. `pip install -r requirements.txt`

## ansible tower
1. `vagrant init ansible/tower`
1. `vagrant up --provider virtualbox`
1. `vagrant ssh`
